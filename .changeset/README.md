# Changesets in PingSystem

Hello! This folder contains files managed by [Changesets](https://github.com/changesets/changesets).

## How to add a changeset

Run `npx changeset` (or `npm run changeset`) in the root directory and follow the interactive prompts to describe your changes.
